# Jump World Cup — contest forecasting

**Before forecasting any contest card, read [PIPELINE.md](PIPELINE.md) first** — it is the runbook (the exact sequence + the rules). The code in `src/worldcup_props/` alone produces wrong numbers without those rules (stale DB, undercounted stars, SOT mispriced vs deep blocks).
